{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "npm --workspace @reputon/frontend run build",
  "installCommand": "npm install --no-audit --no-fund",
  "outputDirectory": "frontend/.next",
  "regions": ["iad1"],
  "env": {
    "NEXT_TELEMETRY_DISABLED": "1"
  }
}
